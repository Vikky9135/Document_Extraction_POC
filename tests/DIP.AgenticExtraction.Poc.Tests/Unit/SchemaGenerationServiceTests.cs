using FluentAssertions;

namespace DIP.AgenticExtraction.Poc.Tests.Unit;

public class SchemaGenerationServiceTests
{
    [Fact(Skip = "TODO Step 17 — assert GPT-5 schema generation from sample OCR text.")]
    public void GenerateFromSamples_discovers_fields()
    {
        true.Should().BeTrue();
    }
}
