using FluentAssertions;

namespace DIP.AgenticExtraction.Poc.Tests.Unit;

public class CorrectionLoopTests
{
    [Fact(Skip = "TODO Step 17 — assert iteration count and merge logic (maxIter).")]
    public void CorrectionLoop_reextracts_only_failed_fields()
    {
        true.Should().BeTrue();
    }
}
