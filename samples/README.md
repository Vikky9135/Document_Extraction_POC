# Samples

Place sample inputs and expected schemas here for local testing.

Suggested files (from the plan):

- `schema-subscription-form.json` — mirrors DIP Core ClassTypeID=7 ExtractionFields
- `schema-invoice.json`
- `test-invoice.pdf`
- `test-subscription.pdf`

These are used by the integration tests in
`tests/DIP.AgenticExtraction.Poc.Tests/Integration`.
